/**
 * Reserved component slot — when AnalysisStep data is wired through
 * the API, render the framework strip here. Until then this file
 * exports a no-op so the import surface stays stable.
 */
export default function StoryAnalysis() {
  return null
}

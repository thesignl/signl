/**
 * Reserved layout slot. Section-level navigation is currently
 * handled by per-page filters (FrameworkTabs, LearningTracks); this
 * file remains as a stable export for future page-level chrome.
 */
export default function SectionNav() {
  return null
}
